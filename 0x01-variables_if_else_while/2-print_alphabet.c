#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - main prints the alphabet in lowercase using putchar function
 * Returns: 0 is a succes
*/

int main(void)

{
char ch;

for (ch = 'a'; ch <= 'z'; ch++)
purchar(ch);
purchar('\n');

return (0);
}
