#include "main.h"

/**
 * main - check for uppercase
 * @y: number to be checked
 * return: 1 for upper letter or for else is 0
 */

int _isupper(int y)

{
	if (y >= 65 && y <= 90)
	{
	return (1);
	}
	return (0);
}
