#include <stdio.h>
/**
 * main - prints function using printf and the return should be 0
 * return value o
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
