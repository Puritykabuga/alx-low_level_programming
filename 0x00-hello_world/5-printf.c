#include <stdio.h>
/**
 * prints main function using printf and the return should be 0
 * returns(0)
 */
int main(void)
{
printf ("with proper grammar, but the outcome is a piece of art,");
return(0);
}
