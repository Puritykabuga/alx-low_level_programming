#include <stdio.h>
/**
 * main - writes a program that prints a line using function puts
 * Return: 0 - when successful return 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}

